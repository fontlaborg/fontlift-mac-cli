# TODO.md
<!-- this_file: TODO.md -->

## Bigger ideas

- [ ] Add a CI job that runs the `.ttc` collection dedup case in `list -p -n` as an
      explicit regression test (a collection appears once in path-only mode but multiple
      times in name mode — verify both).
- [ ] Add `/// - Parameter` doc comments on the public Swift functions exercised by the
      integration tests.
- [ ] Publish the `docs/` Jekyll site to GitHub Pages (enable Pages on the `docs/` folder
      of the default branch) and add a docs badge to the README.
